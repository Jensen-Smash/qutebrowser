[permissions]
allow = ["Bash=cat > \"$HOME/probe_fav.py\" <<'PY'\nfrom types import SimpleNamespace\nfrom qutebrowser.qt.widgets import (QApplication,QMainWindow,QPushButton,\n                                    QVBoxLayout,QWidget)\nimport qutebrowser.mainwindow.toolbar as tb\n\nclass Mgr:  # mimics BookmarkManager.marks\n    def __init__(self, marks): self.marks=marks\n\napp=QApplication([])\nwin=QMainWindow(); win.resize(900,700)\nc=QWidget(); l=QVBoxLayout(c); c.setStyleSheet(\"background:#eceff1\")\nbtn=QPushButton(\"收藏夹\")\nl.addWidget(btn); l.addStretch(1); win.setCentralWidget(c); win.show()\nstub=SimpleNamespace(window=lambda: win)\npop=tb.FavoritesPopup(stub)\n\nfor n in (0,1,3,12,31):\n    marks={f\"https://e{n}_{i}.example/\":f\"书{i}\" for i in range(n)}\n    tb._bookmarks_manager=lambda m=marks: Mgr(m)\n    pop._refresh()\n    if pop.isVisible(): pop.hide()\n    pop.show_at(btn)\n    app.processEvents()\n    sb=pop.list.verticalScrollBar()\n    visible_rows=pop.list.count() if n==0 else min(n,pop.list.count())\n    print(f\"n={n:>2} height={pop.height():>4} scrollbar_visible={sb.isVisible()} listrows={pop.list.count()}\")\napp.exit()\nPY\necho written\n/d/study/Git_Start/qutebrowser/.venv/Scripts/python.exe \"/c/Users/y1787/probe_fav.py\" 2>&1 | grep -v \"stack:\\|Qt\\|Warning\\|GPU\\|context\""]
